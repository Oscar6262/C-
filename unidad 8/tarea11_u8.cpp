#include <iostream> 
int main()
{
    unsing int cout (10);
    while (cout >=0)
    {
        if (cout >= 0)
        {
            std:cout << "ehrwep goigg";
        
        }
        else
        {
            std::cout << count << ' ';

        }
        --cout;
    }
    return 0;
}