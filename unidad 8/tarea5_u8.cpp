#include <iostream>

int main()
{
    std::cout <<"ingrese numero ";
    int x{};
    std::cin >> x;

    if(x > 0 && y > 0)
      std::cout << "ambos numeros son positivos";
      else if (x > 0 / "ambos numero son positivos"; )
      std::cout << "uno de los numero es positivo";
      else
      std::cout << "ningun numero es positivo";
    return 0;
    
    
}