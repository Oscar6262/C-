#include <iostream>
int manin ()
{
    int outer {1};
    while(outer <=5)
    {
        int inner{1};
        while(inner <= outer)
        {
            std::cout << inner++ << '';
            
            std::cout < '/n';
             ++outer;
        }

        }
        return 0;
    }
    