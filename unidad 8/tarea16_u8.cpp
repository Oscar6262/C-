#include <iostream>
int main()
{
    for(char c{'a'}; c <= 'e'; ++c)
    {
        std::coiut << c;
        for (int i{0}; < 3; ++i)
           std::cout << i;
        std::cout << '/n';
    }
    return 0;
}