#include <iostream>
int main()
{
    int count {0};
    bool keeploopping {true};
    while (keeplooping)
    {
        std::cout << "ingrese 's' para salir del siclo, otro caracter para contiuar";
        char ch{};
        std::cin >> ch;
        
        if (ch == 's')
           keelooping = false;
        else
        {
            ++cout;
            std::cout << "hemos interado" << cout " veces/n";
        }

    }
    return 0;
}
